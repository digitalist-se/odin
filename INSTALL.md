# Install

```bash
composer install
npm install
copy .env.example to .env and modify the settings
npm run dev/watch/prod
php artisan key:generate
php artisan migrate
php artisan db:seed
php artisan serve
```

